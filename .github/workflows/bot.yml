name: Complete Monitoring Bot

on:
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours
  workflow_dispatch:

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - run: pip install -r requirements.txt
      
      - name: Run monitoring bot
        run: python monitoring_bot.py
      
      - name: Calculate analytics
        run: python calculate_analytics.py
      
      - run: |
          git config --local user.email "bot@rayrquant.com"
          git config --local user.name "RAYR Bot"
          git add data/
          git diff --staged --quiet || git commit -m "Monitor: $(date -u +'%Y-%m-%d %H:%M') UTC"
      
      - uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main